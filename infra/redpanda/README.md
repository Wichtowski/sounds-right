# Redpanda

Redpanda provides a Kafka-compatible event log for local development.
Application services use the internal broker address from `KAFKA_BOOTSTRAP_SERVERS`.

The external port `19092` is exposed only for local debugging.
